const token = 'YOUR TOKEN';
const prefix = 'YOUR PREFIX';
const host = 'YOUR HOST/IP';
const port = 'YOUR PORT';
const portNumber = 0000; // This needs to be your port too, but as a number instead of a string.

module.exports = {
    token,
    prefix,
    host,
    port,
    portNumber
}